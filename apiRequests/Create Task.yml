info:
  name: Create Task
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/api/tasks"
  headers:
    - name: x-api-key
      value: test123
  params:
    - name: x-api-key
      value: test123
      type: query
      disabled: true
  body:
    type: json
    data: |-
      {
        "title": "Deploy Anchor"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
