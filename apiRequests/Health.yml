info:
  name: Health
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/health"
  headers:
    - name: x-api-key
      value: test1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
