info:
  name: Get Tasks
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/tasks"
  headers:
    - name: x-api-key
      value: test123
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
